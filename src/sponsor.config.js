// Whitelabel sponsorship. Edit and push to main — GitHub Actions redeploys
// automatically. Leave `name` blank to disable all sponsor UI (rails,
// mobile banner, print footer credit) and ship the app unbranded.
export const sponsor = {
  name: '', // e.g. 'Acme Insurance'
  logo: '', // path under public/, e.g. '/sponsor-logo.svg' — drop the file in public/
  url: '', // optional link-out for the logo/name
  tagline: '', // optional short line under the name (rails only, not the mobile banner)
  accent: '', // optional hex, used only as a thin trim/border accent
}
